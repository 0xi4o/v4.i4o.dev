---
courseTitle: Building with PocketBase
title: 'Users, ownership, and @request.auth'
description: Register and authenticate users via the API, then make records owner-only — about 25 minutes
lessonNumber: 2
createdAt: 2026-08-11
updatedAt: 2026-08-11
---

import { Aside, KeyIdea } from '../assets/LessonLayout'
import { Quiz } from '../assets/Quiz'

**The win for today:** two real users created through the API, each owning
their own posts — with PocketBase itself refusing to let one touch the other's.
Roughly twenty-five minutes, entirely in the terminal.

In [lesson 1](./0001-first-run-and-mental-model.mdx) you proved that rules are
authorisation-as-data. Today the rules get an identity to work with.

<KeyIdea>
	An **auth collection** is just a base collection whose records can log in. Users are rows.
	Registration is a plain record create; login returns a **stateless JWT** — no sessions, nothing
	stored server-side, no logout endpoint. Every rule can then see the caller as `@request.auth`
	([Authentication docs](https://pocketbase.io/docs/authentication)).
</KeyIdea>

## 1. Register two users (5 min)

PocketBase ships with a `users` auth collection. Creating a user is the same
endpoint as creating any record:

```sh
curl -X POST http://127.0.0.1:8090/api/collections/users/records \
  -H "Content-Type: application/json" \
  -d '{"email":"alice@example.com","password":"pass1234567","passwordConfirm":"pass1234567"}'
```

Repeat for `bob@example.com`. (The default create rule on `users` allows public
sign-up — worth noticing: sign-up policy is _also_ just a rule.)

## 2. Log in and hold the token (5 min)

```sh
curl -X POST http://127.0.0.1:8090/api/collections/users/auth-with-password \
  -H "Content-Type: application/json" \
  -d '{"identity":"alice@example.com","password":"pass1234567"}'
```

The response contains a `token`. Stash each user's token in a shell variable
(`ALICE=...`, `BOB=...`). Authenticated requests just add
`-H "Authorization: $ALICE"`.

<Aside>
	No cookie, no session row, no logout endpoint — the JWT is self-contained and verified per
	request. "Logging out" is the client discarding the token. This matters later: the React Router
	app will keep this token in the SDK's auth store, and your Go hooks will see the resolved user
	on every event.
</Aside>

## 3. Give posts an owner (5 min)

In the dashboard, add a field to `posts`: `author`, type **relation**, single,
pointing at `users`. Then set the collection's rules:

```
listRule / viewRule:    published = true || author = @request.auth.id
createRule:             @request.auth.id != "" && @request.body.author = @request.auth.id
updateRule / deleteRule: author = @request.auth.id
```

Read them aloud — they're the whole authorisation story: anyone sees published
posts, owners also see their drafts, you must be logged in to create (and can
only create as yourself), and only owners mutate
([API rules and filters](https://pocketbase.io/docs/api-rules-and-filters)).

## 4. Prove it (10 min)

As Alice, create a post (include `"author"` set to her record id from the
login response). Then try to update it as Bob:

```sh
curl -X PATCH http://127.0.0.1:8090/api/collections/posts/records/POST_ID \
  -H "Authorization: $BOB" \
  -H "Content-Type: application/json" \
  -d '{"title":"bob was here"}'
```

You should get a **404**, not a 403 — records your rules hide from a caller
simply don't exist for them. Repeat as Alice and watch it succeed. Also try
creating a post as Alice with Bob's id in `author`; the create rule blocks it.

## Check yourself

<Quiz
	questions={[
		{
			prompt: 'What does @request.auth refer to inside a rule?',
			options: [
				'The authenticated requesting user',
				"The collection's default superuser",
				'The record being modified',
				"The server's global configuration",
			],
			answerIndex: 0,
			explanation:
				'It is the auth record of whoever sent the request — empty if the caller is anonymous.',
		},
		{
			prompt: 'An updateRule of `author = @request.auth.id` lets whom update?',
			options: [
				"The record's owner",
				'Any signed-in user',
				'Only superusers themselves',
				'Everyone, including guests',
			],
			answerIndex: 0,
			explanation:
				"The rule filters per record: it passes only when the author field equals the caller's id. (Superusers bypass rules entirely.)",
		},
		{
			prompt: 'How does PocketBase track a logged-in user between requests?',
			options: [
				'Stateless JWT tokens',
				'Server-stored session records',
				'Random opaque strings',
				'Rotating refresh tokens',
			],
			answerIndex: 0,
			explanation:
				'Tokens are self-contained JWTs verified per request; nothing is stored server-side and there is no logout endpoint.',
		},
	]}
/>

## Primary source

Read the official [Authentication docs](https://pocketbase.io/docs/authentication)
— the identity flows (password, OAuth2, OTP) in one page, and the source for
everything above.

## Keep for reference

The [mental-model cheat sheet](../reference/pocketbase-mental-model.mdx) now
has an auth section with these endpoints.

## Next

Lesson 3 leaves configuration and enters your territory: importing PocketBase
as a Go library — `main.go`, your first event hook, and a custom route. From
there the course builds the actual backend shape your product needs.

<Aside>
	Questions — why 404 instead of 403, how OAuth2 fits, where token lifetimes are set? Ask me in
	the session. That's what I'm here for.
</Aside>
